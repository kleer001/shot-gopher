# Core dependencies
numpy>=1.20.0

# Mesh processing (mesh_deform.py)
scipy>=1.7.0
trimesh>=3.9.0
Pillow>=8.0.0

# ComfyUI database migrations (required by ComfyUI)
alembic>=1.13.0

# SMPL-X mesh generation (smplx_from_motion.py)
# Note: torch and smplx require separate installation due to CUDA dependencies
# Install PyTorch: https://pytorch.org/get-started/locally/
# Install smplx: pip install smplx

# Camera Alembic export (export_alembic.py)
# The Alembic Python bindings must be installed via conda:
#   conda install -c conda-forge alembic
# Note: The "alembic" package on pip is NOT the same (that's a database tool)

# Web GUI (start_web.py)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
websockets>=11.0
jinja2>=3.0.0

# Development/Testing
pytest>=7.0.0
