# Core dependencies
numpy>=1.20.0

# Image processing (separate_instances.py, texture_projection.py)
opencv-python>=4.5.0
Pillow>=8.0.0

# Mesh processing (mesh_deform.py)
scipy>=1.7.0
trimesh>=3.9.0

# ComfyUI database migrations (required by ComfyUI)
alembic>=1.13.0

# SMPL-X mesh generation (smplx_from_motion.py, export_mocap.py)
# Note: torch and smplx require separate installation due to CUDA dependencies
# Install PyTorch: https://pytorch.org/get-started/locally/
# Install smplx: pip install smplx
# Note: export_mocap.py runs in gvhmr conda env which has chumpy pre-installed

# Alembic export (export_alembic.py)
# Uses Blender headless for mesh-to-Alembic export.
# Blender is auto-installed via the installation wizard.

# Web GUI (start_web.py)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
websockets>=11.0
jinja2>=3.0.0

# Development/Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
